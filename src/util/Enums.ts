export enum StatusKinds {
    requirements = "REQ",
    changes = "CHG",
}

export enum ChangesStatusEnum {
    analise = "AN",
    aprovado = "APR",
    rejeitado = "RJ",
    bloqueado = "PR",
    emImplementação = "ING",
    implementado = "IED",
    verificando = "VE",
    finalizado = "D",
}
