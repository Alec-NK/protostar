import styled from "styled-components";

export const Container = styled.div`
  .body {
    display: flex;
    flex-direction: row;
    width: 100%;
  }

  .main {
    display: flex;
    flex-direction: column;
  }
`;
